//
//  {{ module_info.name }}.swift
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import SwiftUI
import UIKit

// MARK: - {{ module_info.name }}Configurator
struct {{ module_info.name }}Configurator: Configurator {
       
// MARK: - Methods
    static func createScreen<Content: View & Presentable>(withView view: Content,
                                                          configureBlock: ((Content?) -> ())?) -> UIViewController {
        
        guard let view = view as? {{ module_info.name }}View else { fatalError("view does not comply with protocol") }

        view.presenter.router = {{ module_info.name }}RouterImpl()
        
        configureBlock?(view as? Content)
        
        return UIHostingController<ContainerView<{{ module_info.name }}View>>(rootView: ContainerView(content: view))
    }
    
}